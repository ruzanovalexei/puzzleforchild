name: puzzleforchild
description: Мозаика. Животные
version: 1.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15 # Добавьте эту строку
  yandex_mobileads: ^7.18.0
  # ... остальные зависимости ...

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/pets/
    - assets/images/farm_animals/
    - assets/images/wild_animals/
    - assets/images/african_animals/
    - assets/asset_lists/ # Добавляем путь к папке со списками ассетов